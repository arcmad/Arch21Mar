alert("hi")
console.log(Math.ceil(Math.random() * 10));

var dice1=prompt("Enter the number in the first dice:<any number less than or equal to 6>:")
var dice2=promt ("Enter the number in the second dice:<any number less than or equal to 6>:")
var totalCount = parseInt(dice1) + parseInt(dice2);
alert(totalCount.toString());